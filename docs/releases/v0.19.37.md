# v0.19.37 — ThreeD Projects: Save + Restore State

Prepared checkpoint release; package `0.19.37`. User reviewed and accepted the implemented scope. Production deployment is pending; current confirmed production remains v0.19.36.

Save Project now captures selected asset, Project Assets-panel visibility and search/type filter, Controls visibility, Physics Debug visibility and Project-specific named camera views, alongside the existing marker, camera/map, environment, ground and layer state. Optional validated additions preserve version-1 snapshot compatibility. Missing selected assets/type references fall back safely during restoration.

Both Save entry points capture the underlying workspace even when the Project dropdown temporarily hides it. New saves leave the dropdown closed on reload. The Save icon is beside the top-left Project dropdown, retaining its busy indicator and existing save handler. Responses from a departed Project cannot change the incoming Project's save feedback.

DetailsCards stay hidden and non-interactive until ThreeD presentation completes. Ordinary 2D behavior remains available without waiting for a ThreeD Scene.

Named views are database-backed with the Project snapshot (up to 50 bounded views). The previous global localStorage key is retained untouched, but its views are not automatically imported into Projects. Recreate desired views in the Project and Save Project to persist them.

Validation passed: TypeScript, Project settings round-trip/bounds and actual Save capture regression, Project-session checks, 51 Runtime Marker groups plus source refresh checks, movable-ball contact checks and diff review. The Rapier fixture emits an existing initialization-parameter deprecation warning. User accepted the browser checkpoint; separate production build output was not supplied.

No schema migration or database command is required. Existing owner-scoped snapshot transactions, persistent Canvas/Rapier, Character runtime separation and animation/world-effect boundaries remain intact. No build, commit or deployment performed by the agent; User owns production build/deployment.

The broader save-state plan continues in future stages: additional Library/DetailsCard state, full Model orientation audit, dirty-since-save feedback, viewport adaptation and legacy view import. See the [development record and coverage matrix](../plans/v0.19.37.md).
