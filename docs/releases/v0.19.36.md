# v0.19.36 — Dashboard Home: Project Discovery UI/UX

Prepared for production release. User reviewed and accepted the development checkpoint; package `0.19.36`. Production deployment is pending; current confirmed production remains v0.19.35.

The Dashboard home page provides compact Project discovery with All Modules before Search, quick navigation above Project cards, outline Open Project buttons and a subtle 4px page inset. Quick links are ThreeD Scenes, Multimedia Library and Traffic Monitor; existing destinations remain intact.

Project cards correctly count active saved Scene assets and reusable assignments separately. Both count queries explicitly correlate the outer Project identity and owner, fixing incorrect zero counts for populated Projects.

Loading validates HTTP responses and pagination, cancels obsolete requests and provides in-place Retry. Search and module filters cover loaded Projects; Clear Filters and Load More support discovery without losing existing cards when an additional page fails. Keyboard focus, accessible action names, coarse-pointer touch sizing and reduced-motion styling are preserved.

Validation passed: `npm run validate:dashboard-project-discovery`, `npm run typecheck` and `git diff --check`. Regression coverage includes generated count SQL, response validation, pagination, merge/filter, HTTP errors, retry, retained cards and cancellation. User accepted the visual checkpoint. These checks do not replace the production build gate.

No schema migration is required. API authorization, shared Dashboard layout and ThreeD Scene runtime remain unchanged. User owns the build and deployment gates; no build, database command, commit or deployment was performed during release preparation.

See the [development record](../plans/v0.19.36.md).
