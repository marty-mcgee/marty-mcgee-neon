# v0.20.12 — ThreeD Models: FarmBots + Assembly Groups

Release preparation for the User's announced deployment. Package `0.20.12`; production deployment and the User-owned build are not yet confirmed.

- Add the in-memory Assembly definition/component/placement contract and transform validation.
- Add a Front-End local composition workspace with Model selection, relative transforms, repeated components, duplication/removal and JSON draft import/export. Access it from Scene Setup in a separate tab.
- Add ThreeD Assembly in Admin for future Model assignments; position Assembly Groups last in the Models submenu.
- Define the approved Assembly, assignment, revision and component tables in the existing ThreeD Drizzle schema. Add ORM demo creation/cleanup services and offline validation.
- Run Drizzle CLI scripts explicitly through Node with strict confirmation to address the reported Bun prompt failure. Full Bun removal is deferred to v0.20.13.

This checkpoint does not yet provide live 3D assembly construction, Project assembly placement, a database-backed save/load UI or confirmed seeded demo data. The editor labels compositions as local drafts. No FarmBot JS/MQTT/device integration is introduced.

Schema application is unconfirmed. The reported push plan includes unrelated existing-table changes that must be reviewed separately before application. No agent schema push or data mutation was performed. The ORM services are not yet exposed as public routes.

Implementation validation includes TypeScript, assembly contract/persistence query mocks, shared UI boundaries, Model placement and Project-session checks. Live database constraints, rollback and deployment behavior are not established by offline tests. Build remains User-owned.

Commit title: `v0.20.12 — ThreeD Models: FarmBots + Assembly Groups`

See [implementation record](../plans/v0.20.12.md), [persistence status](../plans/v0.20.12-persistence/README.md), and [v0.20.13 Bun Removal plan](../plans/v0.20.13.md).
