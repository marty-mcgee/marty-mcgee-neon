# v0.19.19 — Character Collisions + Interactions

Production deployment of the checkpoint is User-confirmed. Repository release commit: `0fa1ebe`; package `0.19.19`. Follow-up corrections below remain local and are not covered by that deployment confirmation. No separate manual build result was reported.

## Released checkpoint

- Walking arrival bounds exclude selection highlights while retaining actual target geometry.
- Initially hidden Layers disable their fixed and Character physics bodies through the existing synchronization path.
- Teleport queries exclude disabled colliders and bodies immediately after Layer changes.

User confirmed hidden objects allow passage, visible objects restore support, and Kate/Joey can walk or teleport toward each other as actor and Action Target. Keep the accepted movement and collider behavior unchanged unless a defect is demonstrated.

## Follow-up corrections

- Character saves reject nearby overlapping spawns using the existing reload thresholds, including persisted coordinate precision. Previously, position writes checked exact equality and full snapshots had no overlap check. Offline validation passed; User accepted the follow-up improvements and authorized production release.
- Model Action Target availability uses Runtime Marker instance identity instead of comparing it with reusable Model IDs. The User confirmed the tractor targeting fix works. Production release of this correction is authorized; deployment completion remains to be verified.
- Joey's Restore Source Position worked: he returned to (4, 0, -20), behind the Farmhouse. This was successful recovery outside the previous camera view, not a rendering defect.

## Validation and remaining review

Recorded passing checks: TypeScript, walking, teleport, hidden collision initialization, animation restart, assigned animations, Character position saving, orchestration, Runtime Markers and the actual target-availability effect fixture. These are local/offline checks, not live SQL or complete world-action acceptance. See the [implementation plan](../plans/v0.19.19.md) for per-step evidence.

The User accepted the follow-up improvements and requested documentation and production release. TypeScript, Runtime Markers, Character position-save and target-availability checks passed again before release. The User owns the manual build gate; no agent build or database operation was performed. Verify deployment completion separately from the Git push.

Scope remains collisions and target interactions. No new world actions, schema, pathfinding, automatic Garden behavior or device commands. The older missing FBX regression fixture remains documented in [v0.19.18](v0.19.18.md).
