# v0.19.15 — ThreeD Admin Pages: UI Updates

Production deployment and manual `npm run build` are User-confirmed. Released commit: `9157e06`; package: `0.19.15`.

Completes the planned Models-blueprint rollout across Plants, Beds, Plantings, Characters, Layers, FarmBots, Waterings and Harvests. Compact headers, independently scrolling records and consistent pagination preserve specialized forms and embedded Project sizing. CRUD lists add server search/sorting and confirmed page-local bulk deletion. Waterings provides separate read-only Schedules and History views.

TypeScript and 13 offline validators passed. The User supplied successful compilation, TypeScript, page-data collection, 110/110 static pages and final optimization output. Deployment confirmation does not assert every individual production smoke test. The agent did not run the build or deploy.

No schema or dependency change. Model Files remains specialized; Waterings editing/execution, form-selector limits, dedicated Model animation assignment editing and other documented follow-ups remain separate work. See the [release handoff](../plans/v0.19.15-release.md), [workspace blueprint](../plans/admin-threed-workspace-blueprint.md), and [implementation record](../plans/admin-threed-workspace-continuation.md).
