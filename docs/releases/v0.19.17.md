# v0.19.17 — ThreeD UI Refinement + Scene Overlays

**Production deployment User-confirmed.** Feature commit `e16e2e4`; documentation and package-version correction commit `edefa4d` also released to production, User-confirmed. Package version is now `0.19.17`, resolving the initial release-label mismatch. A separate local manual build result was not reported; the agent did not run a build.

This checkpoint refines ThreeD Admin forms and front-end controls with compact spacing, responsive sections, readable labels and bounded scrolling. Tasks adopts the shared workspace header and scrolling-records layout. Other App modules, including Music and Traffic, were excluded.

The accepted Scene milestone keeps the Project dropdown anchored beneath the Project name and above activity panels. Project Assets and Model, Character, FarmBot, Bed and Planting panels overlay the Scene without changing Canvas dimensions. DetailsCard retains adjacent positioning when an activity panel is open.

TypeScript and diff checks passed. Per-step comparisons verified static CSS-only edits where applicable; reviewed JSX exceptions cover Tasks layout and Scene container margins. User confirmed the visual checkpoints, including stable Canvas framing and visible placement guidance. This does not claim exhaustive browser/device testing.

No schema, API, animation/physics runtime or world-action changes are included. Legacy Marker CRUD remains unused and retained for separate cleanup; its incidental styling was removed before release. Character behavior and world-action UX remain deferred.

See [implementation and review record](../plans/v0.19.17.md).
