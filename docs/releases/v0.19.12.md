# v0.19.12 — ThreeD Admin Model Workspace

**Prepared for production; deployment pending.** Package `0.19.12`. Proposed commit title: `v0.19.12 — ThreeD Admin Model Workspace`. Prepared September 11, 2026. Last confirmed production remains v0.19.11 (`9cdc78e`).

- Paginated Admin Models with server search/sorting, total counts, compact bulk controls and a stationary workspace around independently scrolling records.
- FBX shared Texture substitutions that link the original file, full importer queue reset, clear status colors and a scrollable queued-files panel.
- Current Model/File references after generic Model and Planting position saves, plus verified DetailsCard repair links.
- Admin Models designated as the [blueprint for other Admin ThreeD pages](../plans/admin-threed-workspace-blueprint.md); rollout is future work.

All 16 selected local gates passed, including TypeScript, assets, pagination and importer/Scene regressions. The User accepted the UI checkpoint. No build, database operation, push or deployment was run by the agent. No new schema changes are required; the build remains the User's manual gate.

See the [complete release handoff](../plans/v0.19.12-release.md) for implementation scope, validation, production smoke checks, retained limitations and rollback guidance. Record the deployed commit and production confirmation here after release.
