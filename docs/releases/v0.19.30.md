# v0.19.30 — Movable Balls + Placement Refinements

Release candidate, package `0.19.30`. User accepted the checkpoint and requested release preparation. Current confirmed production remains v0.19.29.

## Included

- User-verified ordinary Model Library placement of two different Movable Ball Models alongside Characters in Demo Farm Project 8, using existing proximity-based Environment collision.
- Successful Move Model and Save Placement responses reset the selected instance's form to the returned record, clearing stale drafts and dirty status. Failed requests retain the draft.
- Explicit dynamic Model transforms clear previous linear/angular velocity before applying placement; unrelated bodies retain their state.
- Regression coverage for multiple actors, shared-region accounting, departure and capacity deferral, plus independent ball snapshot transforms and physics settings.

## Validation

Passed `npm run typecheck`, `npm run validate:threed-runtime-markers` (51 groups), `npm run validate:threed-orchestration` (18 groups), `npm run validate:threed-hidden-collision-mount`, `node src/lib/scripts/validate-threed-ball-reposition.cjs`, `node src/lib/scripts/validate-threed-movable-ball.cjs`, and `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types src/lib/scripts/validate-threed-environment-regions.mts`. Diff checks passed.

User confirmed the two-ball experiment and preceding tests working, then accepted the placement correction as a solid checkpoint. Fixtures cover helpers and installed Rapier behavior; they do not establish connected database writes, exhaustive terrain coverage or quantitative performance. The reported smaller-asset responsiveness is an observation, not a proven file-size cause.

## Publishing handoff

1. Run the User-owned `npm run build` gate.
2. Smoke-test Move Model and Save Placement for both balls in Project 15; confirm returned coordinates and Saved state. In Project 8, save/refresh both balls and check restored settings, Character control and layer toggling.
3. Publish through the normal Git/Vercel workflow with title `v0.19.30 — Movable Balls + Placement Refinements`.
4. Confirm deployment afterward.

No schema migration, dependency or environment configuration change is required. No commit, push, tag or deployment was performed by the agent. Saved placement does not freeze the subsequent physics simulation. Existing v0.19.29 region capacity, fallback and teleport-readiness limits remain.
