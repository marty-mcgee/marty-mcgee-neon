# v0.21.1 — ThreeD Documentation + CI Reliability

Prepared release candidate; production deployment is not yet confirmed. Package `0.21.1`.

The README now introduces ThreeD through its features and technology stack, with npm setup and developer details below. Detailed release history and integration plans are linked from their documentation.

GitHub Actions now installs the npm version declared in `package.json` before dependency installation. This addresses the reported validation failure where the runner's npm 11.19.0 rewrote a lockfile prepared with npm 11.6.2. The lockfile check, TypeScript and CI validation group remain enabled.

Verification: a fresh install with Node 24.21.0/npm 11.6.2 left the lockfile unchanged; TypeScript and all 41 offline CI validation tasks passed. README links, npm commands, shell syntax and diff checks passed. The version bump changes no dependency versions or application runtime behavior. Existing audit findings remain unresolved.

The User owns the production build and deployment. A successful hosted GitHub Actions run remains to be confirmed after the workflow is committed and pushed.

Commit title: `v0.21.1 — ThreeD Documentation + CI Reliability`

See the [checkpoint record](../plans/v0.21.1.md).
