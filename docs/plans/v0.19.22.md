# v0.19.22 — ThreeD Model Files: Guided Setup + Default Shapes

Prepared candidate, package `0.19.22`. User approved release preparation; manual build and publication remain pending. See [release notes](../releases/v0.19.22.md). The entries below record development and validation.

## Post-release follow-up: all-format steps and empty thumbnails

User reported Model #5 (FarmBot, GLB) showing only step 2 and an empty image-src warning when expanding attachments. The thumbnail predicate classified by extension alone; it now requires a nonblank string URL, so both list and grid paths use their existing icon fallback for missing URLs.

Model Files gated its inspector to FBX/OBJ despite the shared renderer and assignment API supporting GLB/GLTF materials. Enabled those formats when a primary file is registered. All three headings now remain visible in the split Model Files workspace, with guidance when the Model is unselected, lacks a primary file, or uses a format without material editing. Missing-primary guidance points to the existing attachment upload and Set as primary action. No primary file is selected automatically; the saved-file authority is preserved.

Validation: TypeScript, diff check, new `node src/lib/scripts/validate-threed-model-file-thumbnails.cjs` regression and all 8 offline GLTF material-target groups passed. Browser follow-up: open Model #5, expand attachments in list and grid, verify icons replace blank image URLs and steps 1/2/3 remain present. Register its intended primary Model file via the existing action, then verify GLB appearance controls load. The agent did not alter database records, version, or production deployment.

### Primary-file setup directly in Step 1

User approved replacing indirect attachment-list instructions with a primary-file setup sequence. Added a primary Model file selector and explicit Save primary file action inside Step 1, reusing the existing primary PATCH handler and refreshes. Entries with missing URLs are disabled. Upload Model file accepts Model formats and uses the existing attachment uploader with an explicit model category and models directory, independent of the last supporting-file category. Uploading does not implicitly choose a primary; the user selects and saves it. Step 2 and the bottom status now point to Step 1 when no primary is registered.

TypeScript, thumbnail regression, shared requirement-link fixture and diff checks passed. Browser review: FarmBot #5 → Step 1 → choose an existing valid Model file (or upload one first) → Save primary file; verify refreshed preview, enabled supported appearance controls and updated required-file status. No database mutation, commit or deployment was performed by the agent.

### On-demand notes overlay

User requested less inline explanation and a separate window overlay for notes. Added a Help button in the Model preview header that opens the existing accessible Dialog component. It contains primary-file, global/per-material Texture, supporting-file, OBJ/MTL and saved-status guidance. Removed repeated instructional paragraphs from the main sections and shortened the no-external-files state. Required next steps, loading, errors and save status stay visible. TypeScript and diff checks passed. Browser review: open Help, dismiss it, and verify the main controls remain compact and usable. No upload/save behavior or release version changed.

### Default shape selector in Model Files

User requested the existing default mesh presets in the preview/settings page. Inspection found metadata.fallbackShape and shared model-fallback-core helpers already used by the Model editor and generic renderer (sphere, box, rectangle, cylinder, pyramid, torus). Step 1 now offers these presets with explicit Save shape. It preserves other loaded metadata, uses the authenticated Model PATCH, and reloads Model detail and selector data so the existing preview receives saved metadata. The status reports unsaved shape changes. Help explains that the default applies when the file is unavailable, not in place of successfully loaded geometry. No schema or renderer behavior changes.

Validation: TypeScript, diff checks, and all six preset round-trips preserving unrelated metadata passed. Browser test: Model #5 → Default Shape / Mesh → Box → Save shape; verify fallback preview updates and survives refresh. Verify a Model with loaded geometry retains its authored mesh. No production publication performed.
