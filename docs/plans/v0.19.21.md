# v0.19.21 — ThreeD Model Import + File Workspace Refinements

Released at package `0.19.21`; production deployment is User-confirmed. Earlier stage entries below record implementation history. See the [release notes](../releases/v0.19.21.md) for final scope and validation. A separate manual build result was not reported.

## Stage 1: explicit shared FBX Texture requirements

Screenshots show PolygonNature.png required while PolygonNature_01.png is selected. Bulk Import already implemented explicit substitution but hid it inside a dropdown. Model Files only suggested filename matches; its linking handler used the selected texture filename rather than the required filename.

Added a direct Use selected shared Texture for this requirement button in Bulk Import, retaining its existing preparation/preview/import path. Model Files now offers an active shared-Texture chooser for each missing FBX image requirement and an explicit Link to requirement action. Choices are limited to the same image extension, matching the existing API contract. The link saves the required filename as a Model-relative alias to the original shared URL. Files, dependency audit and Model data refresh afterward. No Blob copy or schema change.

Validation: TypeScript, existing bulk saved-texture fixtures, actual Model Files linking-handler fixture (`node src/lib/scripts/validate-threed-requirement-texture-link.cjs`) and diff checks passed. Requests in the new fixture are mocked; live persistence/preview appearance remains User review.

Review: Bulk Import -> select PolygonNature_01.png -> missing PolygonNature.png -> Use selected shared Texture for this requirement. Model Files #939 -> missing PolygonNature.png -> select PolygonNature_01.png -> Link to requirement. Expect the saved dependency count to refresh and reuse the shared file. Material Base Color assignment remains a separate action; no automatic replacement of unrelated maps.

## Remaining stages

- Explicit mapping of an already uploaded attachment to a differently named requirement, after inspecting its API mutation and URL-sharing/deletion contracts.
- Reconcile preview-only resources, saved dependency state and material assignments in clear UI feedback.
- Review FlowerPatch independently and specialized skybox preview appearance without guessing a texture or changing authored geometry.

Preserve primary File authority, format-specific geometry/MTL checks, inactive deferral, shared URLs and the specialized Model Files editor. This step does not claim support for arbitrary GLTF/OBJ shared aliases.

## Bulk Import progressive disclosure

User redirected work exclusively to simplifying Bulk Import first; Model Files work is paused. The shared Texture selector now sits outside batch settings. Batch defaults, supporting-file inventory, extra attachments and per-Model configuration use collapsed native details sections with count/scale summaries. Model file selection, companion-file selection, shared Texture choice, requirement controls, errors and import footer remain directly available. Copy is shortened; underlying assignments/import handling are unchanged. TypeScript, saved-texture fixtures and diff checks passed. Browser review: open Bulk Import Models and check the shorter default view, expand optional sections, and confirm unresolved requirements still show without expanding settings.

## Bulk Import screenshot order refinement

The annotated review identified the Texture selector appearing before Batch settings and supporting-file/refresh controls competing with the primary file choice. Updated `ThreeDModelsBulkImport.tsx` to present numbered steps: Choose Model files, Batch settings, then Assign Existing Texture File. Batch settings remain collapsible. Texture refresh now lives within the Texture section; the standalone availability count and duplicate Batch defaults heading are omitted. Supporting uploads are inside the collapsed Supporting files section with a short explanation of when they are needed.

Acceptance: the three steps follow the annotated order, optional supporting uploads remain reachable, and Texture loading/errors remain visible. Import preparation, dependency checks, and footer actions are unchanged by this layout step. TypeScript and diff checks passed. Browser review: open Admin → ThreeD → Models → Bulk Import Models, verify the order, then expand Supporting files to find its upload button. Build remains the user's manual gate.

## Model Files — progressive disclosure first pass

After accepting the Bulk Import layout, the user moved focus to Model Files. The current page expanded attachment-directory instructions, a drop zone and full file management alongside the preview and Texture controls. This presentation-only step renames the main panels to Model preview and Required files, shortens supporting copy, collapses Upload supporting files (with nested directory options), and collapses Model files and attachments. Required-file fixes remain visible; upload progress/errors remain outside the collapsed upload section. File-list loading/failure status appears in its summary. A Choose supporting files button makes the expanded upload section directly usable.

Acceptance: preview and missing-file resolution remain available on arrival; optional upload/directory/list tools are expandable without changing saved data. Existing upload, shared Texture linking, material assignment and preview handlers are preserved. TypeScript, diff check, and the explicit shared Texture link validator passed. Browser review pending: select MountainSkybox on Model Files, inspect the quieter initial layout, expand Upload supporting files and Model files and attachments, and verify the required Texture can still be linked. No build, commit, push or deployment performed.

## Model Files — compact appearance controls

Following acceptance of the first pass, inspected the shared preview component and its two hosts. Only Model Files enables the material inspector; the single-Model creation preview does not. The inspector repeated its title inside a tinted nested card and showed usage counts instead of filenames. This presentation pass replaces it with Model appearance, a full-width Base Color Texture selector displaying names and filenames, and Save Texture to all materials with a Saving state. Individual materials (advanced) remains collapsed. A concise note accurately distinguishes the existing single-material preview from saving across all materials and resolving missing dependencies.

No preview, assignment, persistence or loader handlers changed. Acceptance: compact controls fit the right panel, the selection is identifiable by filename, and the save action is explicit. TypeScript and diff checks passed; browser review pending on Model Files. No build or Git publication performed.

### Appearance before required files

User requested Model appearance above Required files. Swapped their responsive order and desktop grid rows in the preview and Model Files components; the preview still spans both rows. Updated the appearance helper to reference Required files by name instead of saying "above." TypeScript and diff checks passed. This is a layout/copy-only change; review the order on desktop and narrow windows.

### Compact numbered settings

User reported excess space between the right-hand appearance and requirements panels and requested global-to-individual numbered settings. The preview spans both desktop grid rows; automatic row sizing allowed its height to expand the first row. Set the first desktop row to min-content and the second to the remaining space, keeping both right panels top-aligned with their normal gap. Numbered the controls: 1. Global appearance, 2. Individual materials (advanced), 3. Required files. Individual controls remain collapsible. TypeScript and diff checks passed; desktop spacing and expanded-material browser review remain pending. No loader, state, or assignment behavior changed.

### Numbered workflow and bottom status

The next annotated review requests Global appearance → Required files → Individual materials and a bottom readiness box. The material inspector previously enclosed both appearance and individual controls, preventing requirements from sitting between them. Added a presentation slot for the existing Required files section and stacked all three panels in the preview's right column, preserving the existing upload and assignment handlers. This replaces the two-row sizing workaround. Required files remain available for formats without a material inspector.

The bottom status uses the saved dependency audit (all requirements actually satisfied, including shared references), file loading/errors, active file mutations and upload failures. It also checks material saving/errors and compares temporary Texture preview references with saved material assignments. Green reads Saved files ready, with a reminder to check appearance; unsaved preview choices and unresolved requirements cannot imply everything is saved. Global selection is checked across the inspected materials. No activation, upload, or save happens from the status box.

Validation: TypeScript, shared Texture requirement-link validator and diff check passed. Browser acceptance pending: verify 1/2/3 order, green on a fully saved Model, attention for a missing reference, preview-review status after selecting another Texture, and the resulting status after saving. No build or Git publication performed.
