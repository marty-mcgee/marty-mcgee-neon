# v0.19.32 — Characters, Models + Animation Groups

Status: released to production, package `0.19.32`, commit `ee508b5`, User-confirmed. User prioritizes accessibility before visual, touch and aural customization.

Release preparation: User accepted the menu-overlap correction and requested production preparation. Candidate title is **ThreeD Animations: Custom Action Slots + Preview Workspace**, covering the completed animation and Dashboard scope. TypeScript, animation-availability fixture, 18 orchestration groups and diff checks passed. See [release handoff](../releases/v0.19.32.md). User has confirmed production; supplied console output confirms schema generation/push and the production build.

## Stage 1 — Shared panel interaction accessibility

Proven gaps: shared panels only outlined summary focus; animation buttons had 24px minimum height and three tightly spaced columns. Changes are scoped to opted-in Dashboard panels in globals.css and the Character animation grid in DetailsCard.tsx.

Implemented visible focus for summaries, buttons, links, inputs, selects, textareas and tabindex controls. Coarse-pointer devices use 44px minimum control targets and a two-column animation grid with 8px gaps; desktop layout stays compact. Forced-color mode uses system surface, border and focus colors. Existing transparency, touch background and reduced-motion behavior remain. No action dispatch, animation mapping, physics, schema or persistence change.

TypeScript and diff checks passed. Manual acceptance: Tab through Character DetailsCard and Project menu; use Enter/Space on groups and actions; inspect touch target layout and scrolling with coarse-pointer emulation, and check system forced colors. No browser or screen-reader certification is claimed.

## Next stages

### Independent Character/Model animation preview

User imported four Soccer clips and requested preview without Action assignment. The shared CharacterAnimationAssignments host now loads every Library page independently of assignment search/category filters and offers a top-level Preview any animation selector. Inactive entries remain visible but disabled; selection uses the existing same-origin preview handshake and saves neither mappings nor presets. Assignment help is folded to reduce visible copy. TypeScript, diff checks and the mocked assignment UI fixture passed, including independent preview/no-write/no-draft assertions. Actual rig compatibility still requires browser preview; this change performs no retargeting.

Animation Mapping Presets capture collections of existing Action assignments. They do not create Action keys. Before the dedicated-slot stage below, LIBRARY_ACTIONS had Pick Fruit and Wheelbarrow actions but no Kick Soccerball slot. Soccer actions extend the established semantic/runtime/DetailsCard contracts; do not repurpose harvesting slots for Soccer playback or physical ball interaction.

### Preview workspace tightening

User annotated excess top-level and Action-row space. Moved independent clip selection into the preview pane, consolidated Help and Mapping Presets into one toolbar, bounded assignment-search width, and gave assignment/preview columns equal desktop width. Action selectors, Preview and Save share a row where space permits, wrapping on narrow screens. Verbose source/state guidance is retained under Assignment details; unsaved and locomotion warnings remain visible. TypeScript, mocked assignment/independent-preview fixtures and diff checks passed. Visual acceptance remains manual for both Character and Model windows, including expanded preset/help sections and narrow screens. No Action keys or playback behavior changed. Follow-up layout adjustment puts Mapping Presets before Assignment Help, with the Character/Model preview on the left and Action assignments on the right. DOM and keyboard order follow the visual layout; narrow screens show preview first. TypeScript and the existing assignment fixture passed after this adjustment.

### Project-menu overlap correction

User screenshot proved Project menu obscures the open Model Library header/filters. The Dashboard coordinator now hides and makes inert the Model/Character/FarmBot Libraries, Project Assets and DetailsCard while Project summary is open. Their components remain mounted with unchanged open/draft state; closing the summary restores them. This affects presentation only, not Canvas/physics or marker state. Project menu Escape closes it and restores trigger focus. TypeScript and diff checks passed. Browser acceptance: open a Library, open Project menu, confirm Library disappears, press Escape and confirm its filters/draft return; repeat with DetailsCard and Assets. No automated browser result is claimed.

- Review Character/Model animation presentation and grouping using existing assignment/runtime contracts.
- Extend accessibility and palette treatment to remaining Scene panels after inspecting their hosts.
- Add User visual/touch/aural preferences only after accessible defaults are established. Preference persistence and aural feedback behavior require a concrete design; no new sound or schema is introduced in Stage 1.

Build/publishing remain User-owned. Changes are uncommitted and no deployment was performed.

### Dedicated Action slots — implementation criteria

Proved: server contracts accept only fixed LIBRARY_ACTIONS, while DetailsCard separately hardcodes task groups. Existing assignments store varchar Action keys without a schema enum, and both Character runtimes already play assigned semantic keys through the established one-shot path. Soccer clips cannot currently be saved under a dedicated Soccer key.

This step adds a shared Soccer slot catalog (left/right kick, pass, header), accepted by assignment/preset contracts and rendered in Character DetailsCards. Users manage these slots using Assign, Disable and Restore defaults in the existing Character/Model editor. Slots must report actual loaded availability, inherit Model mappings, play once and return to locomotion, and never trigger harvesting, ball impulses or target orchestration. Preview remains independent. Generic Model Scene playback remains outside the existing runtime capability. User-created arbitrary slot definitions require a separate persistence design; this stage adds dedicated supported slots without changing schema.


Implemented the four dedicated Soccer slots in `action-slots.ts`, shared by server assignment validation, runtime availability reporting and DetailsCard. The editor supports saving clips, disabling slots and restoring inherited/default mappings using existing APIs and Mapping Presets. Soccer buttons omit target payloads; no world effect is introduced. No schema change is needed.

Validation: TypeScript, assignment editor, library API, Mapping Presets, assigned Character animation loading (including all four Soccer slots, Model inheritance and Character disabling), categories, Character restart and orchestration checks passed. The loading fixture binds a known FBX rig using mocked requests; it does not establish compatibility of the imported Soccer files. Manual acceptance: preview each imported Soccer clip on Kate, save it to its corresponding slot, reload Project #8/#15, check Active/Inactive Soccer groups, play each one-shot with and without a selected target, and verify return to locomotion and Take/Release Control. Test disabling and restoring Model inheritance. The user owns the build gate.

### User-owned Action Slot management — approved replacement

User explicitly approved schema changes. Replace the unreleased hardcoded Soccer catalog with owner-scoped `threed_animation_action_slots` records: immutable opaque key, editable name/group, enabled state. Preserve built-in gameplay keys; custom slots are animation-only. Admin creates/edits/disables/deletes slots in the shared assignment workspace; references block deletion. Assignment and preset writes validate custom-key ownership under locks. Scene reads labels/groups from saved slot definitions and loaded availability from existing runtimes. Existing custom assignments must not silently fall back or be deleted. Schema application and live acceptance remain User gates; rollback must preserve slot records and mappings before reverting code.

Implemented replacement: Manage Action Slots is available in the Animations Library and both Character/Model assignment windows. Users create names/groups, edit them, enable/disable and delete unreferenced slots. New keys are generated opaque identities; renaming never edits assignments. The server validates owner-scoped slot references for assignment and preset writes, locking slots against concurrent deletion. Shared Scene availability now includes loaded custom clips. Custom buttons never carry world-action targets; disabled slots block runtime playback while retaining mappings. Existing built-in gameplay actions remain fixed for their established effects. Generic Model Scene playback remains unsupported; Model defaults are consumed by Characters using that Model.


Rollback: revert application code while retaining the additive table and all assignments. Export slot definitions/mappings before any later table removal; reverting to the prior fixed-action app does not expose custom slots. No live schema push, database query or build was performed by the agent.

Acceptance checklist: create “Soccer Pass” and “Header” in any User-defined group; assign uploaded clips on Kate or her Model; preview, save, reload Project and play once; confirm return to locomotion and no world mutation. Rename and regroup, reload and verify existing mapping identity; disable, reload and verify unavailable actions; try referenced deletion and confirm red conflict, restore/remove mappings then delete. Test owner isolation, both runtime paths, locomotion/control, targeted Water/Pick Fruit and the separate build gate.

Validation of the replacement passed: TypeScript; actual slot service and manager fixtures; Library/assignment/Mapping Preset checks; custom assigned-clip binding/inheritance/slot disabling; bulk import; availability; Character restart and orchestration. Requests/database/UI are mocked where documented. Generated migration was reviewed as additive and includes generic reference recovery. Browser and live schema application remain pending.

### Dedicated Animation Slots Admin page — criteria

Proved: slots currently use embedded managers in the Library and assignment window, with no dedicated route/list controls. This step adds `/admin/threed/animation-slots` with the existing ThreeD Admin header, searchable/sortable records, pagination, page-local bulk selection/deletion, colorized messages, create/edit controls and reference counts. Existing owner-scoped CRUD and protected deletion remain authoritative. Replace embedded managers with links, retain the Scene slot hook, and add Sidebar navigation. No schema/runtime behavior change is needed.

Implemented `/admin/threed/animation-slots` using AdminWorkspaceHeader and the same viewport/table blueprint as the Animations Library. Slot records show icons, Name, Group, Enabled and owner-scoped Model/Character/Preset reference counts. Search, stable sorting and pagination currently operate over the loaded owner-scoped list. Header count reflects filters; selection is page-local, resets across search/sort/paging and excludes referenced rows. Bulk deletion handles independent failures and reports success in green and failures in red; protected server deletes remain authoritative. Create/Edit uses a labeled inline form with retained failed drafts and enabled-state editing. Sidebar, Library and Character/Model preview link to the dedicated page; embedded manager renders are removed. No schema or Character runtime changes in this step.

Validation passed: TypeScript, slot service/manager plus actual new workspace fixtures, assignment editor, Animation bulk fixture and diff checks. Workspace fixture verifies paging/search/sort, referenced-row exclusion, page-local bulk targets, partial failures and create payload with mocked React/network. Live browser and SQL remain manual: visit the route, create/edit/group/disable a disposable slot, assign a clip, confirm reference counts prevent deletion, test filtering/pagination/selection and keyboard/narrow-screen layout. Verify Sidebar Models expansion recognizes the Slots route and preview links reach the page. Existing schema must already be applied; no additional schema change is needed for this step.

### Assignment discovery and labels — criteria

Proved: User-defined slots follow the full built-in list, their group names are absent in assignment rows, custom preview titles/save notices and Preset reviews expose opaque keys, and unmapped custom slots claim existing Scene behavior. Tighten discovery with independent slot name/group filters, place configured slots first, show group/disabled information, and use saved slot names in preview/save/Preset labels. Clip filters and independent preview must remain separate; filtering must preserve drafts and the iframe. No schema or runtime behavior change.

Implemented assignment discovery: configured slots appear first, sorted by their saved group/name, with group and disabled-state annotations. Independent Action Slot name/group filters narrow assignment rows without touching clip search, draft mappings or preview lifecycle. Custom preview headings, save notices and Preset review labels use saved slot names. Unmapped custom slots prompt assignment rather than claiming existing Scene behavior. TypeScript, assignment fixture (including group/search filtering, draft retention and custom preview labels), Mapping Presets and diff checks passed. Visual acceptance remains manual in both Character/Model windows; no schema/API/runtime changes were made in this step. User-owned `drizzle-X/` appeared in the worktree and was left untouched.

### Slot CRUD dialog consistency — criteria

User rejected the unprecedented inline top form. Models uses shared Dialog/Content/Header/Title for Add/Edit. Replace the Slots inline form with that shared dialog, retain header Add Slot and row Edit entry points, show save errors inside it, preserve failed drafts and block closing while saving. List layout must remain stable. No persistence changes.

Replaced the top inline form with the shared Radix Dialog components used by Models. Header Add Slot and row Edit open the same responsive dialog; errors remain red inside it, failed drafts remain and dismissal is blocked while saving. List/pagination layout stays in place. TypeScript, slot/workspace checks and diff checks passed. Browser focus/dismissal/layout acceptance remains manual.

### Shared Animation Categories for slots — approved criteria

User approved replacing free-text group_name with a nullable owner-scoped reference to existing threed_animation_categories. Slot dialogs must offer existing Categories, Uncategorized and quick Category creation. List, assignment filters and Scene sections use Category names. Retain the legacy text column for non-destructive schema push; Category reassignment happens through Add/Edit, preserving slot keys and mappings. Category deletion must not orphan referenced slots. No runtime animation behavior change.

Implemented shared Animation Categories: Add/Edit Slot provides an existing-Category dropdown, Uncategorized and quick New Category creation using the existing authenticated endpoint. Lists, assignment filters and Character sections resolve Category names through owner-scoped joins. Slot saves validate/lock the owner's Category; a composite foreign key prevents cross-owner references and blocks deletion while Slots reference a Category. Move affected Slots to another Category or Uncategorized before deletion. Slot keys, animation mappings and runtime action behavior are unchanged.


Validation passed: TypeScript, actual Slot service and workspace fixtures (including Category creation), assignment editor, Library, Category service, Mapping Presets, assigned Character clip loading and diff checks. Tests use offline/mock dependencies; live schema application and browser acceptance remain pending. Manually verify create/edit Category selection, quick creation, Uncategorized, preserved labels, renamed Categories and protected Category deletion, then refresh the Scene to verify section names.

### Production protocol correction — criteria

Use only `bun db:generate` and `bun db:push` for schema deployment. Retain legacy group_name with its original default/check so schema push cannot discard labels. Category selection remains authoritative; prior labels stay stored for manual recategorization through Add/Edit. Remove obsolete SQL instructions and deleted-artifact test dependencies. Verify TypeScript and release regressions; build remains User-owned.

Release review completed: retained legacy group_name/default/check, removed the deleted SQL-artifact dependency and obsolete manual SQL instructions, and updated the release title/handoff. All checks passed: TypeScript, Slot service/workspace, Character assignments, Library, Categories, Mapping Presets, assigned Character animations, bulk animation import, Character restart, animation availability, orchestration, static assets and diff checks. These are offline/mocked checks as documented; no live database commands or build were run. Root CONTEXT.md was moved by User cleanup; docs/archive/CONTEXT.md was consulted as historical context without restoring it. User archive deletions/moves remain untouched.

### Production confirmation

User confirmed v0.19.32 deployed. Supplied output confirms `bun db:generate`, `bun db:push` and `npm run build` succeeded. Category column/FK were added without dropping legacy Group data. The push also reapplied existing foreign keys and Character animation type/default, and adjusted the Layer config default; execution reported success, with no independent live audit claimed. See release evidence in the completed release notes. No next version is designated.
