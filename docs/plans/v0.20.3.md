# v0.20.3 — ThreeD App Improvements (scope to be determined)

Status: Planning prepared. v0.20.2 is User-approved as a release candidate; production deployment is not yet confirmed. Package remains `0.20.2` so the release commit retains its correct version. Change it to `0.20.3` when this version's implementation starts.

## Starting boundaries

Preserve the v0.20.2 asset/sensor/group inspector separation, Project Assets hierarchy, shared presentation styles and independent toolbar dropdown behavior. Preserve the persistent Canvas/Rapier world, stable marker ownership, generic sensors and groups, Character runtime separation, and explicit camera focus actions.

## Scope selection

No new feature implementation is selected yet. Previously deferred topics remain candidates for discussion, not scheduled work:

- Add to Scene collections and category navigation.
- Whole-Model TransformControls.
- Further FarmBot presentation/integration work, subject to existing phase boundaries.

No schema changes, physical commands, MQTT publishing, or new external services are authorized by this planning checkpoint.

## Stable numeric stages

### v0.20.3.1 — Define the first improvement

- **Proof:** Inspect the selected workflow and record the concrete issue, affected files and acceptance criteria.
- **Act:** Implement the agreed bounded improvement after its scope is selected.
- **Validate:** Run relevant targeted checks and the User's browser acceptance scenario.
- **Resolution:** Pending scope selection. Add subsequent numbered stages as their work is defined; keep assigned numbers stable.

Previous handoff: [v0.20.2 release candidate](v0.20.2-release.md).
