# v0.20.8 — ThreeD Admin: Model Workspace Cleanup

## Status

Released to production successfully, as confirmed by the User. Package `0.20.8`.

## Scope

- Remove six duplicate header navigation links; retain search, preview generation, bulk import and Add Model.
- Show Thumbnail assignment as a green check or gray X without loading images.
- Order columns: Name → Type → Size → Category → Options → Thumbnail → Status → Active → Actions.
- Order row actions: Edit → Files → Animations & Preview → More. Use consistent 32px buttons, neutral default icons, colored hover/focus and accessible labels.
- Put Open Original Model File and Delete Model in More, separated visually.
- Soften Default badges and evenly space Admin navigation/theme/account controls.

## Validation

TypeScript passed after the final code change. shadcn/ui boundary and diff checks passed during release preparation. User accepted the browser presentation. No schema/API changes, dependencies or environment edits; no database commands or installation required.

## Commit message

```text
v0.20.8 — ThreeD Admin: Model Workspace Cleanup

- Simplify the Models header and reorder table columns
- Add a Thumbnail presence indicator without loading images
- Refine row action icons, grouping and hover/focus colors
- Soften Default badges and align Admin header controls
```

## Original production handoff (deployment now User-confirmed)

1. Run `npm run build` and confirm success.
2. Review and commit the checkpoint, then deploy through the existing workflow.
3. Smoke-test Models search/sorting, Thumbnail indicators, Edit/Files/Animations, More actions and Admin header controls.
4. Confirm production deployment. If required, redeploy v0.20.7; no data rollback is needed.

## References

- [Implementation and acceptance](v0.20.8.md)
- [Release notes](../releases/v0.20.8.md)
