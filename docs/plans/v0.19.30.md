# v0.19.30 — Movable Balls in Demo Farm

Status: released to production, package `0.19.30`, titled **Movable Balls + Placement Refinements**. Deployment User-confirmed on September 16, 2026. See [release notes](../releases/v0.19.30.md).

## Proven baseline

User placed two different Soccer Ball Models in Demo Farm Project 8 and enabled Movable ball physics. Character interaction with both Models is User-confirmed working. Screenshot diagnostics show preparation ready, 31,812 total indexed regions, 8,742,468 reference bytes, 39–40 active regions and 38,973–39,053 active triangles, zero deferred regions and no fallback required at those sampled positions. These are samples, not peak memory or whole-Scene coverage measurements.

The User observed faster loading and interaction with the smaller asset. File size alone does not establish the cause of physics responsiveness; geometry, settings and Scene workload must be considered separately.

## Stage 1 — Multiple-actor regression coverage

Problem: `validate-threed-environment-regions.mts` claimed multiple-actor coverage but previously exercised one actor per selection. The current runtime already collects enabled dynamic and kinematic bodies without requiring control or Model identity.

Acceptance: Character and two separated balls contribute regions independently; shared regions count once; a departed ball releases distant regions while other actors retain contact; capacity deferral is explicit and respects limits. No runtime or schema change is needed for this stage.

Implemented these policy fixtures. Region selection, native movable-ball contact and TypeScript checks passed. The native fixture does not load the actual farm or either production ball asset.

## Stage 2 — Terrain and obstacle contact

In `/dashboard/map?projectId=8`, interact with each ball around buildings, fences and terrain transitions. Allow one ball to travel away while the Character remains near the other. Inspect activeTriangles, deferredRegions and fallbackRequired at both locations. Record any tunnelling, unsupported ground, region churn or frame stalls with the selected Model and settings before changing collision behavior.

## Stage 3 — Compare physical settings

Use the existing Ball physics controls to compare mass, friction, bounce and rolling resistance, changing one value at a time. Compare both Models at similar visible dimensions and with matching physical settings. Preserve per-instance settings and the shared bounded defaults until a reproducible problem justifies a change.

## Stage 4 — Save and recovery

Save the Project after moving both balls and adjusting their settings. Refresh and confirm both instances, transforms, Movable ball flags and physics settings restore. Check Take/Release Control and layer hide/show. Inspect the existing save/load path and add a focused regression check if a concrete restoration gap is found.

Build and deployment remain User-owned. No migration, environment change, commit or deployment is included in this development step.

## Save/recovery validation follow-up

User confirmed the preceding tests working. Added a two-Model snapshot round-trip fixture to `validate-threed-runtime-markers.mts`: the real parser, JSON persistence boundary and Runtime Marker builder retain separate identities, positions, `scaleMultiplier`, radian `rotationYInstance`, Movable ball mode and independent physics settings. All 51 Runtime Marker validation groups and TypeScript passed; diff checks passed. This covers helper behavior, not connected database writes or live-position capture from the running Rapier world. No runtime correction was required by this fixture.

## Movable-ball placement correction

User reported Move Model, Save Placement and Unsaved changes problems in Soccer Project 15. Inspection proved the editor's local state initialized once while successful CRUD responses changed its comparison props, leaving old values marked dirty. Successful selected-marker reconciliation now increments a client form revision; only that instance's editor resets to the returned record after Save or Move. Failed requests preserve the draft.

Explicit pending transforms now clear dynamic-body linear/angular velocity before repositioning. Existing bodies and unrelated markers remain mounted. The actual physics-step callback fixture (`node src/libraries/scripts/validate-threed-ball-reposition.cjs`) passes selected-body placement, velocity reset, unrelated-body isolation and one-time application. TypeScript and hidden-layer collision checks passed. Live acceptance remains: in Project 15, Move each ball separately, verify returned coordinates/Saved state, then edit and Save Placement and confirm the other ball is unaffected. Physics may subsequently move a ball under gravity or contact; Saved denotes the saved placement/settings, not a frozen simulation.
