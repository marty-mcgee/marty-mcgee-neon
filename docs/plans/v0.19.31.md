# v0.19.31 — ThreeD Dashboard UI/UX

Status: development, package `0.19.31`. Production v0.19.30 is User-confirmed. Focus applies to the shared Front-End Dashboard Map across Projects.

## Stage 1 — Collapsible actions and shared surface colors

Proven problem: Character DetailsCard rendered all semantic action categories expanded; the DetailsCard, Project menu and Scene Controls used inconsistent black/theme backgrounds. Affected files: DetailsCard.tsx, ProjectHeaderMenu.tsx, ThreeDScene.tsx and globals.css.

Acceptance: animation actions and existing categories can be folded with keyboard-accessible controls; categories show counts; selecting a different Character resets its fold state. Shared panels use a legible dark-slate surface and consistent border. Preserve action dispatch, target filtering, control, physics and stable Scene ownership.

Implemented native details/summary controls for Animations and its semantic groups, initially collapsed. Added an explicitly opted-in shared panel class for DetailsCards, Project menu, Scene Controls and Scene loading overlay. Focus outlines are scoped to those panels. TypeScript and diff checks passed. No browser layout or contrast measurement was performed; visual acceptance remains manual in both Farm Project 8 and Soccer Project 15, including theme modes and narrow screens.

## Stage 2 — Animation availability and categories

Inspect the existing runtime clip inventory and saved assignment categories. Define Active/Inactive precisely from actual assignment/clip status rather than inferring it from button disablement (which currently reflects interaction readiness). Present available and unavailable animations in separate foldable groups while retaining semantic action routing. No new playback/world mutation behavior is authorized by this UI stage.

Implemented runtime evidence for the existing semantic action categories. Garden reports through its existing `findClip` resolver (including blocked assignments); Ecctrl reports its direct task-action map, matching its established task playback path. Token-based cleanup preserves newer reports and scopes evidence by Character ID and Model URL. No assignment or Project data is written. Preview-only Garden instances do not publish reports.

DetailsCard splits each existing action category into Active (playable by loaded runtime) and Inactive (unavailable) sections, with counts, differentiated color and disabled unavailable buttons. Pending evidence is shown as waiting, not a claim that all actions are inactive. Active does not mean currently playing. Existing target-capability filtering and action events remain. Saved Library categories and a full clip browser remain future work; this pass uses existing semantic groups.

Validation passed: TypeScript, `validate-threed-animation-availability.mts` (stable snapshots, isolation and cleanup), Character animation restart/first-pose fixtures and 18 orchestration groups. Browser acceptance remains: open Kate and another Character in Farm/Soccer, inspect category counts, exercise an Active action and verify Inactive buttons remain disabled; switch Characters and confirm groups reset. This fixture does not certify real asset availability or visual appearance.

## Stage 3 — Remaining shared panels

Settled the User-approved DetailsCard transparency treatment: outer surface and sticky header are transparent at rest; hover or focus within supplies an 82% slate background. Inputs/selects retain a stronger fill and text uses a subtle shadow. Touch devices retain the readable slate surface because hover is unavailable; reduced-motion users receive no transition. Global menus retain the stronger shared background. TypeScript and diff checks passed; browser visual acceptance remains separate. This changes backgrounds, not element opacity, so controls stay visible and pointer handling remains intact.

Review the Model/Character Libraries, Add to Scene, Project Assets and Tour overlays before extending the shared surface palette. Preserve responsive layout, overlay stacking and pointer isolation. Avoid global CSS overrides of unrelated Admin components.

Build and deployment remain User-owned. No database schema or environment change, commit or deployment is included.
