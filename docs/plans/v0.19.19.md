# v0.19.19 — Character Collisions + Interactions

**Development preparation complete; feature implementation has not started.** User designated version `0.19.19` for planning and development. Package bumped to `0.19.19`. Current production remains v0.19.18, feature commit `95c3a93` and documentation commit `d6e4068`, both User-confirmed deployed. The user selected the title **Character Collisions + Interactions**. The stages below focus on collisions and target interactions; they are not a claim that new runtime behavior has been accepted.

## Baseline and concrete findings

The worktree was clean before preparation. Read the current project context, v0.19.18 release record and deferred [Character behavior/world-action UX plan](character-behavior-world-action-ux.md). Inspected current DetailsCard control/action labels, Dashboard completion handlers, GardenCharacter and orchestration file boundaries.

- `DetailsCard.tsx` already offers Take/Release Control, target navigation and animation-only messaging. Audit its existing conditions before adding more messages.
- `dashboard/map/page.tsx` already distinguishes successful watering/harvest persistence from an animation that completed but could not save its result. Preserve that distinction and completion-gated writes.
- GardenCharacter and EcctrlCharacter remain separate runtimes. Their control and locomotion behavior must not be inferred from one another.
- Walk to Target and collider-aware Teleport near Target were user-tested with Kate and Joey. Preserve them, including explicit Character identity, ground checks, occupied-landing rejection and saved orientation.
- The older FBX material-target validator still references a deleted fixture. Production asset validation passed for v0.19.18; missing regression coverage is a separate known issue, not permission to restore a user-deleted asset.

## Planned stages

### 1. Audit Character collisions and target approach

Inspect Character capsule dimensions, environment/target collider ownership, movement collisions, and the relationship between rendered geometry and physical boundaries. Trace Admin configuration → resolved Project Character → runtime routing → DetailsCard state. Trace action request → animation completion → optional API write → user feedback. Produce a small state/effect matrix and reproduce any mismatch before changing behavior. Include controlled Ecctrl, released Ecctrl and Garden automatic behavior separately.

Primary files: `src/components/map/details/DetailsCard.tsx`, `src/app/dashboard/map/page.tsx`, `src/components/map/ThreeDScene.tsx`, `src/components/threed/shared/GardenCharacter.tsx`, `src/components/threed/shared/EcctrlCharacter.tsx`, and `src/lib/types/character-actions.ts`.

Acceptance: document which collider owns each tested contact, reproduce any overlap, snagging or incorrect approach distance, and verify that each displayed status and available action has a concrete source of truth; identify confirmed defects separately from desired future capabilities. Do not describe configured Wander as proof that a released Ecctrl Character automatically wanders.

### 2. Fix reproduced collision and proximity defects

Make one scoped correction at a time based on Stage 1 evidence. Check Character-to-environment, Character-to-target and Character-to-Character contact, including rotated targets and approach/teleport clearance. Preserve existing collider ownership and continuous Scene physics; do not replace a collider system or expand Garden movement as an incidental fix.

Acceptance: the specific overlap, obstruction or distance error is reproduced before editing and covered by a focused regression. Retest normal WASD, walking, teleport, saved transforms and both Characters afterward.

### 3. Clarify target interactions and DetailsCard feedback

Make control ownership, selected target, navigation state and action effects understandable with concise existing-state feedback. Clearly distinguish an animation-only action from one that can record Project data. Avoid duplicate notices and preserve the compact overlay design.

Acceptance: the user can tell whether they control the Character, why a button is disabled, and whether an action can write data. Labels use real supported capabilities, without exposing internal runtime names or introducing new eligibility rules.

### 4. Address reproduced interaction completion/recovery defects

Use the Stage 1 evidence to make incremental fixes for one-shot completion, return to the appropriate locomotion/idle state, cancellation, and failed persistence feedback. Add a focused failing fixture before each behavioral fix. Preserve existing action ownership and crossfades; do not manipulate mixers through a second path.

Acceptance: animation completion is not reported as saved data until the existing API succeeds; cancellation does not write data; repeated completion does not duplicate writes. Navigation interruption, Take/Release Control, pose gating and position/orientation saves keep working.

### 5. Regression and release review

Run relevant TypeScript, animation restart, assigned-animation, position-save, navigation, teleport, orchestration and Runtime Marker checks. Use `docs/agents/VALIDATION.md` for the validation ladder. Address the missing FBX fixture only as a separate test-maintenance step that preserves its original skinned-mesh/material assertions; do not silently skip coverage or restore the deleted file.

Provide direct App links and exact visible controls for each local test. Record user observations separately from offline results. The user owns `npm run build`; the agent must not run it. The user-selected title is fixed; final release scope and readiness are recorded from completed work.

## Scope boundaries

ThreeD only. No schema changes, live database operations, shared asset copies, runtime consolidation, navigation mesh/pathfinding, new Garden movement, new harvest/watering persistence, MQTT or physical-device commands. Any expansion of those capabilities needs its own explicit scope decision. Preserve the persistent Canvas/Rapier world, stable marker identity, anchored Project menu and existing fallback visuals.

## Initial local review

Open `/dashboard/map?projectId=8`. Select Farmer Kate → Take Control → select an Action Target through its DetailsCard → return to Kate. Compare the control, target and navigation messages; repeat with Joey. These inspection steps require no save or world action.

For a later Water/Pick Fruit test, name the exact target and explain the existing watering/harvest record it will create before asking the user to invoke it. Avoid vague instructions about “disposable assets.” Garden behavior must be tested with an actual Character routed to GardenCharacter, not by toggling a reusable Character's settings merely to manufacture a test.

## Preparation validation

Preparation changes package metadata and documentation only. Check JSON/version, documentation links, diff whitespace and TypeScript. No runtime edits, build, database calls, commit or deployment are part of this preparation step.
