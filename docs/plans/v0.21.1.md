# v0.21.1 — ThreeD Documentation + CI Reliability

## Status

Prepared release candidate at the User's request. Package and npm lockfile version: `0.21.1`. v0.21.0 remains the confirmed production checkpoint until the User confirms deployment. No commit, tag, push or deployment was performed by the agent.

## Scope

- Lead the main README with product features and the technology stack, followed by npm setup, commands, architecture and documentation links.
- Keep Assembly local-draft and FarmBot read-only limitations explicit; replace the lengthy inline release timeline with documentation links.
- Make GitHub Actions install the npm version declared in `package.json` before `npm install`, preserving the lockfile-diff check and all application validations.
- Document the npm version requirement in the local development guide.

## Proven CI failure and correction

GitHub Actions [run 36258269407](https://github.com/marty-mcgee/marty-mcgee-neon/actions/runs/36258269407), commit `5a5a653`, installed dependencies successfully but failed the lockfile-diff gate. The runner used Node 24.21.0 and npm 11.19.0; the repository declares npm 11.6.2. npm changed peer metadata and optional dependency entries. TypeScript and the CI validation group were skipped, so this failure did not report an application test or Vercel build failure.

The workflow now explicitly installs the npm version from `package.json` before dependency installation. It retains `npm install` and the lockfile check. An isolated fresh install with Node 24.21.0 and npm 11.6.2 left the committed lockfile unchanged; TypeScript and all 41 CI validation tasks passed with that toolchain. Shell syntax and diff checks passed. The modified workflow still needs a new GitHub run after commit/push; no remote rerun or deployment was triggered by the agent.

## Release verification

The scoped implementation was verified before this version-only bump: fresh installation under Node 24.21.0/npm 11.6.2 preserved the lockfile, TypeScript passed, and all 41 offline CI validation tasks passed. README local links, npm script names, shell syntax and diff checks passed. Dependency versions and application runtime code are unchanged by this checkpoint.

The production build remains User-owned; v0.21.1 build acceptance and a successful GitHub-hosted run are not yet confirmed. The agent did not rerun the build or change the active dev server/cache. The existing dependency audit findings and Three.js/postprocessing peer warning remain separate follow-up work.

Commit title: `v0.21.1 — ThreeD Documentation + CI Reliability`
