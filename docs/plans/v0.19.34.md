# v0.19.34 — ThreeD Blueprints for Admin ThreeD Models, Animations, Animation Categories, Animation Slots, + ThreeD Characters

Status: released to production, User-confirmed. Package `0.19.34`, commit `bb1c0d1`.

## Prove

Models and Characters already use shared workspace headers/table/dialog patterns. Animations still renames in its Name cell and reports operation errors through an uncolored notice. Categories and Slots have compact dialogs and tables but related navigation varies. Affected files are the five Admin workspaces and release documentation.

## Acceptance criteria

Animation row Edit opens a shared compact dialog, retains failed drafts and blocks closing while saving. Operation failures are red; success messages are green. Models, Characters, Animations, Categories and Slots offer direct related-workspace navigation without changing header heights unnecessarily. Preserve compact preview layout, identities, assignment precedence, protected deletion, all runtime paths, physics and schema.

Implement incremental changes, validate TypeScript and matching offline checks, then record outcomes. Build remains User-owned. No schema change, live database command, commit or deployment is part of this step.

## Act and validation

Animation row Edit now opens a compact shared Dialog with name, source/clip context and Update/Cancel. Errors retain the draft in the dialog; closing is blocked while mutation is busy. Existing PATCH identity and rename-only payload are preserved. Operation failures use a separate red alert; successful operations use green status messages. Related header links connect Models/Characters with Slots/Categories and return from those workspaces to Models/Characters. Existing table controls and preview layout remain in place. No schema or runtime changes.

Passed TypeScript, animation bulk, Slot service/workspace, Category service/workspace, Character list, Model list and Character/Model assignment checks plus diff checks. These offline checks do not establish browser dialog focus or header wrapping. Manual development review: rename successfully, simulate a failed save and confirm draft/error retention, use Escape/Cancel and keyboard focus, inspect related links and header wrapping on desktop/narrow screens across all five workspaces. This checkpoint is in development, not production-ready by declaration. Build remains User-owned.

## Next refinement — proved criteria

User tested/accepted the prior checkpoint. Character forms establish p-4/gap-3 dialogs and h-8 inputs; Animation/Slot/Category dialogs use larger default padding and inconsistent input heights. Scope changes to these three dialogs and labels/focus in the five workspaces. Preserve coarse-pointer touch targets and scrolling, all mutations and runtime behavior. Shared workspace-link focus styling benefits related headers without imposing global viewport constraints.

Implemented dialog density alignment for Animations, Categories and Slots: p-4/gap-3, h-8 text-xs inputs/selects and compact dialog actions. Coarse-pointer media overrides retain 44px minimum input/select/button heights in those dialogs. Shared workspace links now have explicit keyboard focus rings and coarse-pointer minimum height. Models search has an accessible label; Character Create cannot dismiss while submitting. TypeScript, Slot/Category workspace/service, Animation bulk and Character list checks plus diff checks passed. Desktop/touch focus and density acceptance remains manual; no schema/runtime changes or build performed.

## Completed checkpoint

User confirmed v0.19.34 released to production. Earlier pending acceptance entries are historical and superseded by this confirmation. No separate build console result was supplied; automated validation remains recorded above. No next version is designated.
