# v0.19.10b — ThreeD Model Bulk Importing: production handoff

Status: **prepared for production deployment**, September 9, 2026. Package version: **`0.19.10-beta`**. The User manually tested and accepted the final checkpoint, including the inline preview below Queued Models, and requested production release preparation. The local build gate is **manually passed by the User**; the User explicitly instructed the agent not to run `npm run build` again in this chat.

Production remains [v0.19.10a](../releases/v0.19.10a.md), commit `16a92ae`, until beta deployment is confirmed. No beta commit, push, deployment, migration or environment change was performed during this handoff.

## Release scope

- Mixed FBX, GLB and GLTF bulk importing with embedded resources, external textures and required binary buffers.
- Existing reusable Base Color Texture assignments with batch inheritance, per-Model overrides and None preserving original materials.
- Always-expanded form sections, highlighted file requirements, remembered per-User browser defaults, scale shortcuts and immutable submitted settings beside results.
- On-demand interactive preview beneath Queued Models, with refresh/reset/close and an optional authenticated separate window. Preview uses local selected files and effective transforms/materials before upload.
- Bounded local GLTF decoding before import, canonical material targets, verified primary/attachment/assignment records, explicit activation and guarded recovery. Missing geometry cannot be deferred; deferred missing textures keep the Model inactive.

See the [implementation record](v0.19.10b-implementation.md) for file ownership, resource limits and detailed validation. This handoff adds documentation only and preserves the manually accepted application code.

## Acceptance and verification

| Gate | Evidence |
|---|---|
| User manual acceptance | “I have manually tested this code checkpoint. It is working.” Recorded September 9, 2026. This is checkpoint acceptance, not an invented per-asset test inventory. |
| Local build | User-controlled manual gate, confirmed 100% passing the local instance. No further agent build runs in this chat. |
| TypeScript | `npm run typecheck` passed with no diagnostics during release preparation. |
| Native importer checks | All seven scripts passed: import contract 8, preparation 37, runner 31, FBX targets 4, GLTF bundles 19, GLTF targets 8, preview snapshots 9: **116 groups**. |
| Static assets | Passed: 35 animation assets, 3 DRACO decoder assets and 1 environment asset. |
| Browser proof retained from implementation | Actual FBX/GLB/GLTF loading, textures/DRACO, inline and popup rendering, missing resources, preferences, responsive form, immutable completed results, explicit retry and no preview mutation requests. APIs were mocked for automated browser checks. |
| Version and CI | Package is `0.19.10-beta`; Admin footer reads the package version. CI keeps Assets and TypeScript plus all seven importer validators. Dependencies and lockfile are unchanged. |
| Diff and documentation | Scope reviewed; whitespace, relative links and CI script wiring checked. No generated files or secrets added to the release. |

The previously started release build had already completed when the User's no-build instruction arrived. Its historical result does not replace or expand the User-controlled manual gate.

## Commit and deployment handoff

Suggested commit/release title:

```text
v0.19.10b — ThreeD Model Bulk Importing
```

Include the reviewed beta application changes, new local loaders/preview route and helpers, validators and original fixture source, package/CI updates, this handoff and current documentation. Preserve the already prepared alpha production-history documentation. New source and fixture files are currently untracked and must be included with the commit; do not omit them by committing only modified tracked files. No files have been staged by this preparation step.

Commit the reviewed checkpoint and push through the normal GitHub `main` → Vercel workflow. Require the GitHub **Assets and TypeScript** check and Vercel production deployment to succeed. No database migration, new environment variable, dependency installation change or external resource provisioning is required by this release.

After deployment, confirm sign-in, the beta footer, FBX/GLB/GLTF import and preview, saved Model Files/textures after refresh, and one unaffected Project/Character path. Retain the detailed [asset and regression checklist](v0.19.10b-implementation.md#production-smoke-and-future-regression-checklist) for recording live results, including near-limit uploads. The User's local acceptance is recorded separately from production verification.

Only after successful production confirmation, add `docs/releases/v0.19.10b.md`, update the release index/current-production pointers, and record the deployed commit. Do not mark beta deployed from this handoff alone.

## Limits and rollback

Retain 4 MiB per selected file, 100 queued Models, 500 companion/resource entries, 32 MiB bundle/decoded-geometry limits and bounded image/decoder work. GLTF support is the documented tested subset; OBJ, ZIP/folder ingestion, larger-file transport and durable queue recovery remain deferred. FBX local preview adds no TGA decoder.

If a deployment regression requires rollback, restore the known successful alpha Vercel deployment for `16a92ae`. Keep newly created Model/File/Texture records for review: rolling back application code does not undo completed imports. No destructive data cleanup is part of rollback.
