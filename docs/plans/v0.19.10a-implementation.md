# v0.19.10a Stage 3 — FBX bulk-import implementation

Implemented September 9, 2026. This is development work; no deployment or real upload was performed. Existing Stage 1/2 documentation changes were preserved. Package version remains `0.19.10-alpha`.

The User subsequently accepted the FBX/Texture implementation as a solid alpha checkpoint and requested deployment preparation. See the [alpha deployment handoff](v0.19.10a-release.md) for current release status and the [beta plan](v0.19.10b.md) for GLB/GLTF work. Specific production smoke checks remain unconfirmed.

## Result

The dedicated `/admin/threed/models` workspace now offers **Bulk Import FBX**. Select FBX files, add a shared pool of local textures, review per-Model matches and settings, then import ready rows. Preparation does not upload or persist anything. Blocked rows remain available for correction, and Models explicitly deferred for texture configuration remain inactive/pending.

Names, transforms, category selections and flags are validated before import. Defaults can be overridden per Model and restored to inheritance. Same-named files retain separate identities; ambiguous textures require a choice. Valid destination directories preserve the existing exact-path/unique-filename runtime behavior. Additional texture attachments are supported when an FBX contains no discoverable named references.

The User subsequently requested **Assign Existing Texture File** during bulk preparation. It is now available in **Batch defaults** and each Model's settings. Select one active saved Texture for the batch; each Model can inherit it, select another, or choose **None**. Import applies that Texture to every supported Base Color material slot without uploading another Texture file or requiring later per-Model assignment.

Imports execute sequentially through existing owner-scoped endpoints. Primary creation includes the single-Model `primaryFile` payload, creating the Model and primary attachment together. The runner saves one attachment per request, reads back the primary and attachments, and audits saved dependencies. Activation is off by default and requires explicit opt-in plus a successful dependency audit. Structural analysis still happens in the upload endpoint; reference scanning is not proof of valid geometry or complete materials.

Every known created Model has a **Review Model files** link that opens a new tab, retaining the current batch. Refreshing the Models list after a run keeps the bulk component mounted. The Project-embedded CRUD keeps its existing single-Model workflow to avoid its parent refresh discarding batch results.

## Concrete implementation decisions

- **4 MiB per file:** this bulk-only ceiling includes primaries, textures and previews, leaving multipart overhead below Vercel's 4.5 MB Function request limit. The single upload route's 256 MiB validation value is not deployed transport capacity. Larger-file support requires a separate upload-transport implementation; this stage does not add it. See [Vercel server uploads](https://vercel.com/docs/vercel-blob/server-upload).
- **Bounded local work:** at most 100 Models, 500 selected texture files, and 500 discovered references per primary. File reads run sequentially across chooser events. Too many references blocks that row rather than silently truncating it to a complete result. Matching uses destination indexes to avoid repeatedly comparing every pair for every reference.
- **Owned previews:** a validated preview is saved after Model creation as an `other` attachment under `previews/<filename>`, then assigned as `thumbnailUrl`. This avoids a detached standalone preview upload if a later operation fails. Preview basenames must differ from detected texture references and selected texture attachments because the existing resolver can also see supportive files.
- **Separate Texture workflows:** a local image reused across Models creates Model-owned attachments. An existing reusable Texture selection creates material assignments referencing that master record. Preparation reads the owned Texture catalog and offers active records; refresh errors or unavailable selected records block affected rows only. The runner rechecks the selection before uploading. The batch does not create Texture master records or expand material channels.
- **Actual FBX material targets:** the upload endpoint's existing single server parse now also uses the shared preview inventory to return `analysis.materialTargets` (`targetKeys`, `materialSlotCount`, `omittedSlotCount`). No extra parse or texture fetch is added. Existing-Texture assignment requires a complete, unique inventory of 1–500 canonical target keys. Missing, empty or truncated inventory triggers staged-primary cleanup before Model creation. Removing the selection permits the existing import/review workflow.
- **Verified assignment before activation:** after primary/attachment verification, the runner uses the existing owner-scoped Base Color assignment API in serial chunks of at most 40 keys and 2,048 UTF-8 JSON bytes. It reads back all selected target/channel/Texture IDs before dependency audit and activation. A failed or uncertain assignment retains the known inactive Model with a review link and no create retry. Named-file requirements remain separate: **Resolve missing texture files later (keep inactive)** still applies the selected Texture now but leaves the Model inactive.
- **Definitive rejection versus uncertain outcome:** a rejected pre-create operation allows explicit correction/retry only when any known staged primary was successfully discarded. Lost, timed-out, server-error, or malformed create responses never automatically retry or delete a potentially committed primary. Known Model IDs remain available for review after attachment or verification failure.
- **Bounded responses:** runner responses are limited to 1 MiB and requests use a 120-second abort deadline. Aborting a mutation is not proof that the server rolled it back.

## Changed implementation files

| File | Responsibility |
|---|---|
| `src/components/admin/threed/models/ThreeDModelsBulkImport.tsx` | FBX queue, sequential scans, shared files, matches, inherited settings, selected-row review, explicit submission, results and review links |
| `src/components/admin/threed/models/model-bulk-preparation-core.ts` | Pure validation, source/destination matching, preview validation, readiness, bounded file policy |
| `src/components/admin/threed/models/model-bulk-import-runner.ts` | Existing API sequencing, primary registration, owned preview, saved-file verification, activation and cleanup/uncertainty handling |
| `src/components/admin/threed/models/ThreeDModelsCRUD.tsx` | Dedicated entry point and Model-list refresh without unmounting the queue |
| `src/lib/services/threed/models/fbx-runtime-inspection-server.ts` | Shared material inventory from the existing FBX parse; additive material targets also appear in the runtime-inspection response |
| `src/app/api/threed/models/upload/route.ts` | FBX-only material-target analysis for bulk assignment; GLB/GLTF analysis unchanged |
| `src/lib/scripts/validate-threed-model-bulk-preparation.mts` | Offline preparation acceptance examples |
| `src/lib/scripts/validate-threed-model-bulk-runner.mts` | Mocked API success, rejection, partial-write and lost-response checks |
| `src/lib/scripts/validate-threed-fbx-material-targets.mts` | Tracked FBX and constructed object fixtures proving material-target parity and bounds |
| `package.json` | Commands for the three new validators |

The implementation changes no database schema, environment file, CLI importer, Model renderer, Project persistence, Character runtime, or physics ownership. Existing assignment authorization and persistence remain unchanged.

## Validation

- `npm run validate:threed-model-import`: passed all 8 existing groups.
- `npm run validate:threed-model-bulk-preparation`: passed all 22 groups, including 500 same-name reference destinations, saved-Texture inheritance/None/overrides, catalog failure and inactive deferral.
- `npm run validate:threed-model-bulk-runner`: passed all 21 mocked-request groups, including 500-slot bounded assignment, stale/invalid selections, incomplete inventory, partial/lost assignment and exact readback verification.
- `npm run validate:threed-fbx-material-targets`: passed all 4 groups: tracked Farmer FBX single-parse/no-fetch parity with the preview inventory, nested/shared/array/skinned materials, the 500-slot omission bound, and empty inventory.
- `npm run typecheck`: passed without diagnostics.
- `npm run build`: passed outside the sandbox, including compilation, TypeScript, all 107 static pages and route optimization. The initial sandbox run remained in compilation for five minutes and was stopped; the same command then succeeded without source or environment-file changes.
- `git diff --check`: passed.
- Independent source review: no remaining material findings after fixing parent unmounting, preflight parity, and repeated suffix-check work.
- Isolated Chromium checks against the real component and shared repository CSS, with every API request mocked: passed preparation-without-writes, duplicate-name texture selection, destination correction, default/override inheritance, mixed ready/blocked/deferred selection, double-submit protection, primary/attachment payloads, inactive deferral, retained results after closing/reopening, and lost create-response handling. Additional checks passed removal during scanning without row restoration, distinct same-named Models, and shared-texture removal recalculating readiness. No browser runtime errors. Desktop and 390px layouts were inspected; a footer overflow was found and fixed.
- Existing-Texture Chromium checks passed batch inheritance across two Models, another Model's override, explicit None/reset, active-only options, no preparation writes, all-slot assignments and activation without Texture re-upload, stale-catalog correction, catalog failure, and assignment failure retaining one inactive Model. The original bulk browser checks also passed after the extension. Desktop and 390px picker layouts were inspected with no horizontal overflow.

The browser harness and its temporary tooling are under `/tmp/threed-bulk-browser`; no browser dependency was added to the repository. These synthetic FBX fixtures exercise orchestration, not real FBX parsing or rendered materials.

## Remaining manual and later-stage work

Before releasing this implementation, import representative real FBX files through the authenticated deployment: bare/relative/duplicate texture names, missing textures, embedded textures, and a corrupt FBX. Verify primary registration, saved attachments, dependency audit, Model preview, explicit activation and Library/Scene placement; repeat single-FBX import/configuration and the relevant Character/Scene regressions. Confirm an actual near-limit multipart upload in the deployment.

For the new saved-Texture path, import two building FBXs with one batch Texture, give another Model a different Texture, and leave one set to None. Confirm saved assignments and rendered Base Color in Model Files and Library/Scene placement. Verify missing named-file dependencies remain visible and do not activate a deferred Model. Offline target parity does not establish appearance for every real FBX.

Queue state is tab-local. Refresh/navigation loses local selections and results; there is no durable batch resume or automatic server reconciliation of an unknown create. Known partial imports are repaired through Model Files. **Correct and retry** is limited to confirmed pre-create failures; uncertain outcomes require checking Models first. **Clear imported rows** clears successful results only. This bounded recovery behavior is the accepted alpha Stage 4 boundary. Further repair/resume improvements remain later work; Stage 5 covers downstream real-asset validation.
