# v0.20.2 — ThreeD Scene Workspace + Inspectors

Status: **Release candidate approved by the User**. Package `0.20.2`. Production deployment is not yet confirmed. A version-specific build result has not been reported; the User owns `npm run build`.

## Commit message

```text
feat(threed): v0.20.2 Scene Workspace + Inspectors

- Separate asset, physics sensor, and sensor group inspectors
- Simplify Project Assets and highlight nested sensor selection
- Add accessible sensor creation and group management controls
- Improve inspector scrolling and narrow-screen presentation
- Add focused inspector rendering validation and user documentation
```

## Included scope

Checkpoints `v0.20.2.1–4`: Project Assets hierarchy, focused inspectors, accessible actions, and presentation layout. Existing sensor placement, transforms, marker persistence and physics behavior remain the underlying runtime paths. Live counts remain available through Environment → Show Sensors.

## Validation evidence

Previously completed for this implementation:

- `npm run typecheck`
- `npm run validate -- threed-inspectors` (rendered markup, not browser interaction)
- `npm run validate -- threed-sensors threed-runtime-markers`
- `git diff --check`

User approval is recorded without claiming separately observed browser results. Retain the [implementation checklist](v0.20.2.md) for smoke testing: selection without camera changes, sensor save/reload, group membership, independent toolbar/Assets visibility, and narrow-screen navigation.

## Handoff

1. Keep package `0.20.2` for this release commit.
2. Run the User-owned build gate if not already completed.
3. Commit the reviewed checkpoint using the message above, then deploy through the normal production workflow.
4. Confirm deployment and run the retained smoke checklist.
5. Begin the [v0.20.3 planning checkpoint](v0.20.3.md); bump the package when its implementation begins.

No database commands or dependency installation are required. No commit, tag, push or deployment was performed by the agent for this handoff.

Help: [Physics Sensors and Sensor Groups](../developers/THREED_PHYSICS_SENSORS.md).
