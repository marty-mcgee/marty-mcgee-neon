# v0.20.5 — ThreeD Scene: Scenarios

Status: release candidate approved by the User, following production v0.20.4. Package version: `0.20.5`. User confirmed the current UI looks good and is ready for production. Production build and deployment are not yet confirmed. See [release handoff](v0.20.5-release.md).

## Purpose

A **ThreeD Scenario** is a Project-scoped arrangement of existing Scene Models, Physics Sensor Cuboids, Sensor Groups, and presentation rules that demonstrates a real workflow. Soccer and Farming are the first reference scenarios.

The Scenario layer explains what the Scene represents. It does not replace Runtime Marker ownership, Rapier physics, Sensor Group storage, ThreeD Layers, or the existing Model Library.

## Classification

| Area | Responsibility | First reference |
| --- | --- | --- |
| ThreeD Physics | Collision, sensor events, trigger/counter observation, transforms | A movable ball entering a sensor |
| ThreeD Soccer | A Scenario that arranges a pitch, balls, sensors, groups, and local match presentation | Home/Away named user metadata |
| ThreeD Farming | A Scenario that arranges Beds, Plantings, FarmBots, environment Models, and read-only observations | Garden/farm workspace |
| ThreeD Scenarios | Project-local setup, guidance, validation, and presentation over existing primitives | Soccer and Farming |

## Initial boundaries

- Scenario names and descriptions are metadata; they do not create special Physics record types.
- “Home,” “Away,” “Goal,” and “Score” remain Soccer metadata and presentation terms, never generic sensor field names or Physics API requirements.
- Scenarios may guide a User to create or connect existing assets. They must not silently create, move, persist, or delete Project assets.
- Local Scenario state must remain separate from persisted Project configuration and from external live-equipment observations.
- FarmBot remains read-only unless a future, separately approved command phase authorizes more.
- No schema work, external service, MQTT publish, or physical-device action is authorized by this roadmap entry.

## Candidate stable checkpoints

### v0.20.5.1 — Scenario contract and eligibility

- **Proof:** Inventory the Project assets and existing generic Physics/Sensor Group capabilities required by Soccer and Farming.
- **Act:** Define a bounded Scenario descriptor and eligibility evaluator using stable marker identities and current metadata.
- **Validate:** Unit validation confirms incomplete Scenarios are reported without mutation and that identical asset IDs across modules remain distinct.
- **Resolution:** A Scenario can describe readiness without owning the Scene.

### v0.20.5.2 — Soccer reference scenario

- **Proof:** A Soccer Project currently relies on manual knowledge of which Models and Sensors form the pitch and scoring locations.
- **Act:** Present a Soccer setup/checklist based on assigned assets and generic counter sensors. Keep user-selected Sensor Group names and sensor behaviors authoritative.
- **Validate:** Browser acceptance covers setup status, sensor entry counts, reset behavior, refresh, and no change to generic Physics functionality.
- **Resolution:** Soccer becomes a documented reference Scenario rather than a Physics-only special case.

### v0.20.5.3 — Farming reference scenario

- **Proof:** Farming assets are individually placeable but lack a shared Scenario-level explanation of their relationship.
- **Act:** Present a Farming setup/checklist using existing Beds, Plantings, FarmBots, environment Models, and read-only live-state conditions.
- **Validate:** Browser acceptance covers missing/available assets, saved positions, and stale/unavailable live observations without creating device commands.
- **Resolution:** Farming becomes a second reference Scenario while preserving the current operational boundary.

### v0.20.5.4 — Scene guidance and presentation

- **Proof:** Scene setup requires context without reintroducing overloaded DetailsCards or competing overlays.
- **Act:** Add optional Scenario guidance through Project Toolbar → Setup → Scenarios and compact read-only Scene presentation. Preserve Project Assets, focused inspectors, and optional Sensors panel responsibilities.
- **Validate:** Verify one toolbar dropdown at a time, Project Assets persistence, narrow-screen access, persistent Canvas/Rapier lifecycle, and no camera change from passive selection.
- **Resolution:** Scenarios improve orientation without changing the underlying Scene controls.

## Version path

1. **v0.20.2 — ThreeD Scene: Workspace + Inspectors**
2. **v0.20.3 — ThreeD Scene: Model Library + Physics Sensor Workflows**
3. **v0.20.4 — ThreeD Scene: DetailsCard Action Toolbar**
4. **v0.20.5 — ThreeD Scene: Scenarios**

Each version remains independently scoped and releasable. Later Scenario work must not be folded into Model Library or Inspector changes.


## Implementation checkpoint — 2026-09-23

- **Proof:** Project Tour described foundation setup but had no Scenario eligibility guidance. Existing Runtime Markers, generic cuboids, Project Sensor Groups and the read-only FarmBot observation hook supply the required data.
- **Act (.1–.4):** Added a pure Scenario evaluator and optional **Setup → Scenarios** overlay. Users explicitly choose the field/environment Model and Sensor Group. Soccer checks active ball Models and at least two group counters; Farming checks active Beds, Plantings and FarmBots. An explicitly selected FarmBot uses the existing observation endpoint and exposes live/stale/disconnected/unavailable status. Closing guidance unmounts its observation polling.
- **Validate:** `npm run validate -- threed-scenarios`, TypeScript and whitespace checks. Fixtures cover missing/inactive assets, cross-module sensor ownership, duplicate owner/sensor pairs, unknown groups and immutable inputs. User subsequently accepted the current checkpoint for release; individual checklist results were not separately reported.
- **Resolution:** Guidance is session-only and read-only. It does not claim that setup eligibility proves collision geometry or sensor entry. No schema, device commands, persistent Scenario records or separate always-visible HUD were added. Compact presentation has its own closable overlay, separate from Project Tour. Both are accessible through Setup; toolbar exclusivity and the independent Project Assets panel are preserved.

Affected implementation: `scenario-core.ts`, `ScenarioGuidance.tsx`, `ProjectSetupPanel.tsx`, and the existing Scene page prop wiring. Canvas/Rapier code is unchanged.

### Browser acceptance

1. Open Setup → Scenarios; confirm Project Assets remains visible and camera position/mode stays unchanged.
2. Choose Soccer, an assigned field Model and a Sensor Group. Verify missing assignments remain “Needed”; two eligible counters with the same group satisfy that check regardless of their names.
3. Inspect each counter using Project Assets; verify entries/reset using Environment → Show Sensors. Refresh and verify persisted positions/groups; guidance selections and session counts are not persisted.
4. Choose Farming. Check missing/available asset statuses; choose a FarmBot and check the read-only observation. Stale/unavailable observations must not be shown as live.
5. Close Scenarios and switch Projects; verify observations stop and choices do not leak to another Project.
6. Check narrow viewport scrolling, keyboard controls and opening Environment/Add to Scene closes either Setup overlay.
7. Run the User-owned production build before release.

See [Scenario help](../help/threed-scenarios.md).

### Toolbar / inspector independence correction

- **Proof:** The Project summary wrapper applied `hidden` and `inert` to Project Assets and DetailsCard. Opening Model/Character/FarmBot/Bed/Planting libraries also cleared marker selection, preventing the inspector from surviving a return to Project Assets.
- **Act:** Removed summary-dependent hiding from Assets and inspectors. Library entry preserves the selected marker. Explicit library panels still replace the left Assets panel; merely opening toolbar dropdowns does not. Opening Project Assets preserves the current marker/sensor inspector.
- **Validate:** `threed-inspectors` now executes the actual Assets/Tour/Environment handlers with setter spies and checks independent visibility. Inspector validation, TypeScript and diff checks pass. User subsequently accepted the current UI checkpoint.
- **Resolution:** Retest with a DetailsCard open: open Project Assets, then Tour, Environment, Add to Scene and the Project summary. Assets and the selected inspector must remain visible; dropdown exclusivity remains separate. Explicit Close or a new selection still controls the inspector.


### Setup menu and separate Scenarios overlay

- **Proof:** Nesting Scenarios in Project Tour mixed reference Scenario guidance with getting-started help.
- **Act:** Setup now contains Project Tour and Scenarios. Each opens a separate closable overlay; opening Setup, Environment or Add to Scene dismisses the previous Setup overlay. Scenario eligibility and read-only observations are unchanged. Assets and DetailsCard remain independent.
- **Validate:** TypeScript, Scenario and inspector regressions; current UI checkpoint accepted by the User. Test Setup → Scenarios → close, Setup → Project Tour, and menu switching with Assets and a sensor inspector open. Confirm camera and selection stay unchanged and observations stop when Scenarios closes.
- **Resolution:** Separate guidance surfaces under one toolbar entry; no schema change or build run.

### Toolbar label polish

Shortened the visible ThreeD toolbar labels from **Add to Scene** to **Add** and **Project Assets** to **Assets**. Menu behavior, panel titles and descriptive accessibility labels remain unchanged. TypeScript and diff checks pass.
