# v0.20.5 — ThreeD Scene: Scenarios

## Release status

User-approved release candidate. Current UI acceptance is User-confirmed. Package version is `0.20.5`. Build success and production deployment remain unconfirmed; no deployment was performed by the agent.

## Validation

- Passed: `npm run typecheck`.
- Passed: `npm run validate -- threed-inspectors threed-scenarios`.
- Passed: `git diff --check`.
- User-owned gate: `npm run build` before publishing.
- No `db:generate`, `db:push`, dependency installation or environment edits needed.

## Commit message

```text
v0.20.5 — ThreeD Scene: Scenarios

- Add Soccer and Farming guidance over existing Project assets and sensors
- Group Project Tour and standalone Scenarios overlays under Setup
- Preserve independent Assets and DetailsCard navigation
- Shorten toolbar labels to Add and Assets
- Add Scenario eligibility and toolbar regression checks
```

## Production handoff

1. Run the production build and confirm success.
2. Commit the reviewed checkpoint and deploy through the existing production workflow.
3. Smoke-test Setup → Project Tour and Setup → Scenarios, Environment, Add and Assets with a DetailsCard open. Verify camera and selection remain unchanged.
4. Check Soccer group readiness, Farming observations and closing Scenarios. Existing saved sensor positions and group membership must remain intact.
5. Record production deployment confirmation. If rollback is needed, redeploy the known-good v0.20.4 deployment; this release requires no database rollback.

## References

- [Release notes](../releases/v0.20.5.md)
- [Implementation and detailed browser checklist](v0.20.5.md)
- [Scenario help](../help/threed-scenarios.md)
