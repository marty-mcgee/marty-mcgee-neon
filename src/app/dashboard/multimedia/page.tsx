import MusicContent from '@/components/multimedia/MusicContent';

export const metadata = {
  title: 'Music by Marty McGee',
  description: 'Albums and Tracks Player',
};

export default function MultimediaPage() {
  return <MusicContent />;
}
