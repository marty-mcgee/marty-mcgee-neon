import { AssemblyCompositionWorkspace } from '@/components/threed/assemblies/AssemblyCompositionWorkspace';

export default function AssemblyGroupsPage() {
  return <AssemblyCompositionWorkspace />;
}
